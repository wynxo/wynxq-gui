# Working preferences

- Work directly on `main` unless the user explicitly changes this instruction.
- For UI changes, run the real application and include fresh screenshots in the
  final response. Show the affected states; do not substitute mockups or old captures.
- Keep scrollbars hidden on idle surfaces. Reveal them on viewport hover,
  scrolling, or thumb dragging, and keep dragging functional outside the viewport.
